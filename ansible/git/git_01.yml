---
 
 - hosts: all
   become: true
   tasks:
    - git:
        repo: https://github.com/mrtoadie/ansible-playbooks.git
        dest: /tmp/ansible-playbooks
        separate_git_dir: /tmp/ansible-playbooks.git
