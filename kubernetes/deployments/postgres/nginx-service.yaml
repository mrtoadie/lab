# nginx-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: test-db
spec:
  selector:
    app: nginx
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP  # Ändere zu LoadBalancer für externen Zugriff
