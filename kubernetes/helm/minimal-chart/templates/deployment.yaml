apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app: simple
  template:
    metadata:
      labels:
        app: simple
    spec:
      containers:
        - name: simple
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.port }}
